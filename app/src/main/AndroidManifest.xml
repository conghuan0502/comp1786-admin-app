<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YogaAdmin">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddCourseActivity" />
        <activity android:name=".activities.ViewCoursesActivity" />
        <activity android:name=".activities.AddInstanceActivity" />
        <activity android:name=".activities.ViewInstancesActivity" />
        <activity android:name=".activities.ManageTeachersActivity" />
        <activity android:name=".activities.CourseDetailActivity" />
        <activity android:name=".activities.EditInstanceActivity" />
        <activity android:name=".activities.ManageInstancesActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.CourseConfirmationActivity" />
    </application>

</manifest>